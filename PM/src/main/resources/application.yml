spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/projectmanagement
    username: ximii
    password: password1
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true